export const getItems = state => state.contacts.items;
export const getIsLoading = state => state.contacts.items;
export const getError = state => state.contacts.items;

export const getFilter = state => state.filter;
